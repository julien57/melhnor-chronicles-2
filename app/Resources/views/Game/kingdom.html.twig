{% extends "game-layout.html.twig" %}

{% block content %}

    <div class="fluid-container">

        <h2>{{ app.user.kingdom.name }}</h2>

        <div class="col-md-6">
            <h3>Bâtiments</h3>
            <div class="row">
                <div class="col-md-5">

                    <form action="{{ path('kingdom') }}" method="post" class="contact-form">
                        <div class="row">
                            <div class="col-md-12">
                                {{ form_widget(buildBuildingForm.building, {'attr': {'class': 'form-control'}}) }}<br>
                                {{ form_widget(buildBuildingForm.construire, {'attr': {'class': 'btn'}}) }}
                            </div>
                        </div>
                        {{ form_rest(buildBuildingForm) }}
                    </form>

                </div>
            </div>

            <div class="row">
                <div class="col-md-6 panel-item__summary justify-content-md-start">

                    {{ form(formBuilding) }}

                </div>
            </div>
        </div>
        <div class="col-md-6">
            <h3>Resources</h3>

            {% for kingdomResource in kingdomResources %}
                <p>{{ kingdomResource.resource.name }} : {{ kingdomResource.quantity }}</p>
            {% endfor %}

        </div>

    </div>

{% endblock %}