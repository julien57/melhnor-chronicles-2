{% extends 'game-layout.html.twig' %}

{% block content %}

    <div class="container">
        <h2>Messagerie</h2>

        <div class="row">
            <a href="{{ path('write_message') }}" class="btn">Ecrire un message</a>
        </div>
        <div class="col-md-10">

            {% for message in listMessages %}
                <div class="row">
                    <div class="col-md-2">
                        <img src="{{ asset('/web/front/img/avatars/' ~ message.recipient.avatar.idAvatar ~ '.jpg') }}" id="avatar_messaging">
                    </div>
                    <div class="col-md-8 text-message messages">
                        <h3>{{ message.subject }}</h3>
                        <p>De {{ message.recipient.username }} le {{ message.atDate|date("m/d/Y") }} à {{ message.atDate|date("H:i") }}</p>
                        <p class="message-body">{{ message.message }}<br>
                            <a href="{{ path('write_message', {'idRecipient': message.recipient.id}) }}" class="btn">Répondre</a>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>

{% endblock %}