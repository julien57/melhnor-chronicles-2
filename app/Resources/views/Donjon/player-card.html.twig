{% extends 'donjon-layout.html.twig' %}

{% block content %}

    <h2>Suppression Joueur</h2>

    <div class="row">
        <div class="col-md-6">
            <div class="card p-4">
                <div class="card-body d-flex justify-content-between align-items-center">
                    <div>
                        <p>Etes-vous sûr de vouloir supprimer {{ player.username }} ?</p>

                        <form action="{{ path('deletePlayer', {'id': player.id}) }}" method="post">
                            <input type="submit" value="Supprimer" class="btn btn-danger" />
                        </form>

                        <div class="row">
                            <a href="{{ path('donjon') }}">
                                Non, retourner sur le Dashboard
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}